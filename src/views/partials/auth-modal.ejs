<div id="authModal" class="auth-modal" style="display: none;">
    <div class="auth-modal-content">
        <h3>Inicio de sesión requerido</h3>
        <p>Para continuar con tu reserva y ver el resumen, necesitas iniciar sesión primero.</p>
        <p class="auth-info">Tus datos de reserva se guardarán automáticamente.</p>
        <div class="auth-modal-buttons">
            <button onclick="window.location.href='/login?redirect=/bookings/summary'" class="btn btn-primary">Iniciar sesión</button>
            <button onclick="window.location.href='/register?redirect=/bookings/summary'" class="btn btn-outline-primary">Registrarse</button>
            <button onclick="hideAuthModal()" class="btn btn-secondary">Cancelar</button>
        </div>
    </div>
</div>

<style>
.auth-modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 1000;
}
.auth-modal-content {
    background: white;
    padding: 30px;
    border-radius: 12px;
    text-align: center;
    max-width: 400px;
    width: 90%;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}
.auth-modal-buttons {
    margin-top: 20px;
    display: flex;
    flex-direction: column;
    gap: 10px;
}
.auth-modal-buttons button {
    width: 100%;
    padding: 10px;
    border-radius: 6px;
}
.auth-info {
    margin-top: 15px;
    font-size: 0.9em;
    color: #666;
}
</style>